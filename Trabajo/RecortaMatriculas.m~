function [] = RecortaMatriculas()
    clear all
    clc

    cd RPtrain
    NIVEA = 29;
    A = imread(['Coche' num2str(NIVEA,'%03d') '.jpg']);
    figure, imshow(A,[])
    cd ..

    B = rgb2gray(A);
    figure, imshow(, [])
    G = imnoise(B, 'gaussian');
    figure, plot(G)
end